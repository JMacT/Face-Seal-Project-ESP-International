/*
                dash_Holder = line.substr(0,4);

                CS_Inch = line.substr(x,x);
                CST_Inch = line.substr(x,x);
                ID_Inch = line.substr(x,x);
                IDT_Inch = line.substr(x,x);

                CS_mm = line.substr(x,x);
                CST_mm = line.substr(x,x);
                ID_mm = line.substr(x,x);
                IDT_mm = line.substr(x,x);
